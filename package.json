{
  "name": "fit-flow",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "vite --port 8080 --host",
    "build": "vite build",
    "build:dev": "vite build --mode development",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^5.2.0"
  }
}
